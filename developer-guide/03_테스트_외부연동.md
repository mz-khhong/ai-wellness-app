# 테스트 및 외부 연동 가이드

이 문서는 AI Wellness App 프로젝트에서 테스트 작성 방법과 외부/내부 서비스 연동 방법을 설명합니다.

## 목차

1. [테스트 작성 가이드](#1-테스트-작성-가이드)
2. [MockData 사용법](#2-mockdata-사용법)
3. [외부/내부 서비스 연동 (Feign Client)](#3-외부내부-서비스-연동-feign-client)
4. [외부 시스템 연동 패턴](#4-외부-시스템-연동-패턴)

---

## 1. 테스트 작성 가이드

### 1.1. 테스트 실행

```bash
# 전체 프로젝트 테스트 실행
./gradlew test

# 개별 모듈 테스트 실행
./gradlew :admin-server:test
./gradlew :manager-server:test
./gradlew :customer-server:test
```

### 1.2. 테스트 리포트 확인

테스트 실행 후 리포트는 다음 경로에서 확인할 수 있습니다:

```
{module}/build/reports/tests/test/index.html
```

### 1.3. 테스트 코드 구조

```
{server}-server/src/test/java/com/aiwellness/{server}/
├── fixture/
│   └── MockData.java              # 테스트용 Mock 데이터 생성
│
├── application/
│   └── service/
│       └── {Domain}ServiceTest.java  # Service 단위 테스트
│
└── domain/
    └── model/
        └── {domain}/
            └── {Domain}Test.java  # Domain 단위 테스트
```

### 1.4. Service 테스트 예시

```java
@ExtendWith(MockitoExtension.class)
class AdminServiceTest {
    
    @Mock
    private AdminRepositoryPort adminRepositoryPort;
    
    @Mock
    private PasswordEncoder passwordEncoder;
    
    @InjectMocks
    private AdminService adminService;
    
    private Admin mockAdmin;
    
    @BeforeEach
    void setUp() {
        // MockData를 사용하여 테스트용 객체 생성
        mockAdmin = MockData.createAdmin();
        mockAdmin = Admin.builder()
                .id(1L)
                .facilityGroupId(1L)
                .uuid("test-uuid-123")
                .email(mockAdmin.getEmail())
                .name(mockAdmin.getName())
                .password(mockAdmin.getPassword())
                .status(mockAdmin.getStatus())
                .createdAt(mockAdmin.getCreatedAt())
                .updatedAt(mockAdmin.getUpdatedAt())
                .build();
    }
    
    @Test
    void getAdmin_존재하는_관리자_조회_성공() {
        // Given
        Long id = 1L;
        when(adminRepositoryPort.findById(id))
                .thenReturn(Optional.of(mockAdmin));
        
        // When
        AdminResponse result = adminService.getAdmin(id);
        
        // Then
        assertThat(result).isNotNull();
        assertThat(result.getId()).isEqualTo(id);
        assertThat(result.getEmail()).isEqualTo(mockAdmin.getEmail());
    }
    
    @Test
    void getAdmin_존재하지_않는_관리자_조회_실패() {
        // Given
        Long id = 999L;
        when(adminRepositoryPort.findById(id))
                .thenReturn(Optional.empty());
        
        // When & Then
        assertThatThrownBy(() -> adminService.getAdmin(id))
                .isInstanceOf(AdminBusinessException.class)
                .hasMessageContaining("admin.not.found");
    }
}
```

---

## 2. MockData 사용법

### 2.1. MockData 구조

```java
package com.aiwellness.{server}.fixture;

import com.aiwellness.{server}.domain.model.{domain}.{Domain};
import com.aiwellness.{server}.domain.model.enums.{Domain}Status;
import java.time.LocalDateTime;

public class MockData {
    // 기본 Mock 객체 생성
    public static {Domain} create{Domain}() {
        return {Domain}.builder()
                .email("{domain}@wellness.com")
                .name("{Domain} User")
                .password("password123")
                .status({Domain}Status.ACTIVE)
                .createdAt(LocalDateTime.now())
                .updatedAt(LocalDateTime.now())
                .build();
    }
    
    // 커스텀 이메일로 Mock 객체 생성
    public static {Domain} create{Domain}WithEmail(String email) {
        return {Domain}.builder()
                .email(email)
                .name("{Domain} User")
                .password("password123")
                .status({Domain}Status.ACTIVE)
                .createdAt(LocalDateTime.now())
                .updatedAt(LocalDateTime.now())
                .build();
    }
}
```

### 2.2. MockData 사용 예시

```java
@BeforeEach
void setUp() {
    // 기본 Mock 객체 생성
    mockAdmin = MockData.createAdmin();
    
    // 커스텀 이메일로 Mock 객체 생성
    String customEmail = "custom@wellness.com";
    Admin customAdmin = MockData.createAdminWithEmail(customEmail);
}
```

### 2.3. 도메인 단위 테스트

```java
class AdminTest {
    
    @Test
    void builder_테스트() {
        Admin admin = Admin.builder()
                .id(1L)
                .email("admin@example.com")
                .name("관리자")
                .build();
        
        assertThat(admin.getId()).isEqualTo(1L);
        assertThat(admin.getEmail()).isEqualTo("admin@example.com");
        assertThat(admin.getName()).isEqualTo("관리자");
    }
    
    @Test
    void noArgsConstructor_테스트() {
        Admin admin = new Admin();
        assertThat(admin).isNotNull();
    }
    
    @Test
    void allArgsConstructor_테스트() {
        LocalDateTime now = LocalDateTime.now();
        Admin admin = new Admin(1L, 1L, "uuid", "admin@example.com", "관리자", 
                "password", AdminStatus.ACTIVE, now, now);
        
        assertThat(admin.getId()).isEqualTo(1L);
        assertThat(admin.getEmail()).isEqualTo("admin@example.com");
    }
}
```

---

## 3. 외부/내부 서비스 연동 (Feign Client)

### 3.1. Feign Client 개요

- **내부 서비스 연동**: admin-server, manager-server, customer-server 간 호출
- **외부 서비스 연동**: 결제, 이메일, SMS 등 외부 API 호출

### 3.2. 폴더 구조

```
{server}-server/src/main/java/com/aiwellness/{server}/
├── adapter/
│   └── infrastructure/
│       └── client/                    # Feign Client
│           ├── admin/                 # Admin Server 호출
│           │   └── AdminServiceClient.java
│           ├── manager/               # Manager Server 호출
│           │   └── ManagerServiceClient.java
│           └── customer/              # Customer Server 호출
│               └── CustomerServiceClient.java
│
└── domain/
    └── port/
        └── external/                   # 외부 서비스 Port
            ├── AdminServicePort.java
            ├── ManagerServicePort.java
            └── CustomerServicePort.java
```

### 3.3. 구현 방법

#### 1단계: Domain Port 정의
```java
package com.aiwellness.{server}.domain.port.external;

/**
 * Admin 서버 호출을 위한 Port 인터페이스
 */
public interface AdminServicePort {
    AdminResponse getAdmin(Long id);
    AdminResponse createAdmin(AdminCreateRequest request);
}
```

#### 2단계: Feign Client 구현
```java
package com.aiwellness.{server}.adapter.infrastructure.client.admin;

import com.aiwellness.{server}.domain.port.external.AdminServicePort;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

/**
 * Admin 서버 호출을 위한 Feign Client
 */
@FeignClient(
    name = "admin-service",
    url = "${services.admin.url}",
    path = "/api"
)
public interface AdminServiceClient extends AdminServicePort {
    
    @GetMapping("/admins/{id}")
    @Override
    AdminResponse getAdmin(@PathVariable Long id);
    
    @PostMapping("/admins")
    @Override
    AdminResponse createAdmin(@RequestBody AdminCreateRequest request);
}
```

#### 3단계: Adapter 구현 (외부 호출 로그 포함)
```java
package com.aiwellness.{server}.adapter.infrastructure.client.admin;

import com.aiwellness.{server}.domain.port.external.AdminServicePort;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Repository;

/**
 * Admin 서버 호출 Adapter
 * 외부 서비스(Feign Client) 호출을 담당하며, 헥사고날 구조 파악을 위한 로그를 포함합니다.
 */
@Slf4j
@Repository
@RequiredArgsConstructor
public class AdminServiceAdapter implements AdminServicePort {
    
    private final AdminServiceClient adminServiceClient;
    
    @Override
    public AdminResponse getAdmin(Long id) {
        log.info("[Adapter/Infrastructure/Client] AdminServiceAdapter.getAdmin() - 외부 서비스 호출 시작: service=admin-service, method=getAdmin, id={}", id);
        try {
            AdminResponse response = adminServiceClient.getAdmin(id);
            log.info("[Adapter/Infrastructure/Client] AdminServiceAdapter.getAdmin() - 외부 서비스 호출 완료: service=admin-service, method=getAdmin, id={}, success=true", id);
            return response;
        } catch (Exception e) {
            log.error("[Adapter/Infrastructure/Client] AdminServiceAdapter.getAdmin() - 외부 서비스 호출 실패: service=admin-service, method=getAdmin, id={}, error={}", id, e.getMessage());
            throw e;
        }
    }
    
    @Override
    public AdminResponse createAdmin(AdminCreateRequest request) {
        log.info("[Adapter/Infrastructure/Client] AdminServiceAdapter.createAdmin() - 외부 서비스 호출 시작: service=admin-service, method=createAdmin, email={}", request.getEmail());
        try {
            AdminResponse response = adminServiceClient.createAdmin(request);
            log.info("[Adapter/Infrastructure/Client] AdminServiceAdapter.createAdmin() - 외부 서비스 호출 완료: service=admin-service, method=createAdmin, id={}, email={}, success=true", 
                    response.getId(), request.getEmail());
            return response;
        } catch (Exception e) {
            log.error("[Adapter/Infrastructure/Client] AdminServiceAdapter.createAdmin() - 외부 서비스 호출 실패: service=admin-service, method=createAdmin, email={}, error={}", 
                    request.getEmail(), e.getMessage());
            throw e;
        }
    }
}
```

#### 4단계: Service에서 사용
```java
@Service
@RequiredArgsConstructor
public class OrderService {
    
    private final AdminServicePort adminServicePort;
    
    public Order createOrder(OrderRequest request) {
        // Admin 서버에서 관리자 정보 조회
        AdminResponse admin = adminServicePort.getAdmin(request.getAdminId());
        
        // 주문 생성 로직
        // ...
    }
}
```

### 3.4. 설정

#### application-common.yml
```yaml
# 서비스 연동 설정 (Feign Client)
services:
  admin:
    url: ${ADMIN_SERVICE_URL:http://localhost:8080}
  manager:
    url: ${MANAGER_SERVICE_URL:http://localhost:8081}
  customer:
    url: ${CUSTOMER_SERVICE_URL:http://localhost:8082}
```

#### 환경 변수
```bash
# 개발 환경
ADMIN_SERVICE_URL=http://localhost:8080
MANAGER_SERVICE_URL=http://localhost:8081
CUSTOMER_SERVICE_URL=http://localhost:8082
```

### 3.5. 에러 처리

Feign Client는 자동으로 에러를 처리합니다:

- **4xx 에러**: `FeignException` 발생
- **5xx 에러**: `FeignException` 발생

```java
try {
    AdminResponse admin = adminServicePort.getAdmin(id);
} catch (FeignException e) {
    if (e.status() == 404) {
        throw new AdminNotFoundException();
    } else if (e.status() >= 500) {
        throw new ServiceUnavailableException();
    }
}
```

---

## 4. 외부 시스템 연동 패턴

### 4.1. 외부 시스템 연동 구조

```
{server}-server/src/main/java/com/aiwellness/{server}/
├── domain/
│   └── port/
│       └── external/                   # 외부 시스템 Port
│           ├── PaymentPort.java
│           ├── EmailPort.java
│           └── SmsPort.java
│
└── adapter/
    └── infrastructure/
        └── external/                   # 외부 시스템 Adapter
            ├── payment/
            │   └── PaymentAdapter.java
            ├── email/
            │   └── EmailAdapter.java
            └── sms/
                └── SmsAdapter.java
```

### 4.2. 구현 예시

#### 1단계: Domain Port 정의
```java
package com.aiwellness.{server}.domain.port.external;

public interface PaymentPort {
    PaymentResult processPayment(PaymentRequest request);
}
```

#### 2단계: Adapter 구현
```java
package com.aiwellness.{server}.adapter.infrastructure.external.payment;

import com.aiwellness.{server}.domain.port.external.PaymentPort;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Repository;

@Repository
@RequiredArgsConstructor
public class PaymentAdapter implements PaymentPort {
    
    private final PaymentApiClient paymentApiClient;  // 외부 API 클라이언트
    
    @Override
    public PaymentResult processPayment(PaymentRequest request) {
        // 외부 API 호출
        return paymentApiClient.processPayment(request);
    }
}
```

#### 3단계: Service에서 사용
```java
@Service
@RequiredArgsConstructor
public class OrderService {
    
    private final PaymentPort paymentPort;
    
    public Order createOrder(OrderRequest request) {
        // 결제 처리
        PaymentResult result = paymentPort.processPayment(request.getPaymentRequest());
        
        // 주문 생성 로직
        // ...
    }
}
```

---

## 5. 체크리스트

### 테스트 작성 시
- [ ] MockData 사용하여 테스트 데이터 생성
- [ ] Service 테스트 작성 (Mockito 사용)
- [ ] Domain 단위 테스트 작성
- [ ] 예외 케이스 테스트 포함

### Feign Client 구현 시
- [ ] Domain Port 인터페이스 정의 (`domain/port/external/`)
- [ ] Feign Client 인터페이스 구현 (`adapter/infrastructure/client/`)
- [ ] Adapter 구현 (`adapter/infrastructure/client/`)
- [ ] `application-common.yml`에 서비스 URL 설정
- [ ] 환경 변수 설정 (운영 환경)
- [ ] 에러 처리 구현

### 외부 시스템 연동 시
- [ ] Domain Port 인터페이스 정의 (`domain/port/external/`)
- [ ] Adapter 구현 (`adapter/infrastructure/external/`)
- [ ] 외부 API 클라이언트 구현
- [ ] 에러 처리 구현
- [ ] 타임아웃 설정 (필요 시)

---

## 6. 주의사항

### 6.1. 순환 참조 방지

- 서비스 간 순환 참조를 피해야 합니다
- 예: Admin → Manager → Admin (X)

### 6.2. 타임아웃 설정

```yaml
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 10000
      admin-service:
        connectTimeout: 3000
        readTimeout: 5000
```

### 6.3. 로깅

#### Feign Client 로깅 설정

Feign Client 로깅은 `FeignConfig`에서 설정됩니다:

```java
@Bean
public Logger.Level feignLoggerLevel() {
    return Logger.Level.FULL;  // 요청/응답 전체 로깅
}
```

#### 헥사고날 구조 파악을 위한 외부 호출 로그

외부 서비스 호출(Feign Client)은 **Adapter 레이어**에서 로그를 기록합니다:

**로그 형식:**
- 시작: `[Adapter/Infrastructure/Client] Adapter명.메서드명() - 외부 서비스 호출 시작: service=서비스명, method=메서드명, 파라미터`
- 완료: `[Adapter/Infrastructure/Client] Adapter명.메서드명() - 외부 서비스 호출 완료: service=서비스명, method=메서드명, 결과`
- 실패: `[Adapter/Infrastructure/Client] Adapter명.메서드명() - 외부 서비스 호출 실패: service=서비스명, method=메서드명, error=에러메시지`

**로그 흐름 예시:**
```
[Application/Service] OrderService.createOrder() - Use Case 시작: orderId=1
[Adapter/Infrastructure/Client] AdminServiceAdapter.getAdmin() - 외부 서비스 호출 시작: service=admin-service, method=getAdmin, id=1
[Adapter/Infrastructure/Client] AdminServiceAdapter.getAdmin() - 외부 서비스 호출 완료: service=admin-service, method=getAdmin, id=1, success=true
[Application/Service] OrderService.createOrder() - Use Case 완료: orderId=1
```

**주의사항:**
- Feign Client 인터페이스는 로그를 직접 찍을 수 없으므로, **Adapter에서 로그를 기록**합니다
- 외부 API 호출(HTTP Client, RestTemplate 등)도 동일한 형식으로 로그를 기록합니다
- 외부 시스템 연동의 경우 `[Adapter/Infrastructure/External]` 형식을 사용할 수 있습니다

